import java.nio.file.Files
import java.nio.file.Paths

rootProject.name = 'base-orchestrator'
rootDir.eachDir {
    if (Files.exists(Paths.get(it.absolutePath, 'build.gradle'))) {
        include ":${it.name}"
    }
}
